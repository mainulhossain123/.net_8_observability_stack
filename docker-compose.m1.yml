
# ── Milestone 1: Structured Logging (Serilog → Seq) ─────────────────────────
# Start: docker-compose -f docker-compose.m1.yml up --build
# Seq UI: http://localhost:8888
# API:    http://localhost:8080/Weather

services:

  # ─── .NET API ────────────────────────────────────────────────────────────
  api:
    build:
      context: ./src/ApiService
      dockerfile: Dockerfile
    container_name: api
    ports:
      - "8080:8080"
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
      - ASPNETCORE_URLS=http://+:8080
      - Seq__ServerUrl=http://seq:5341
    depends_on:
      - seq
    networks:
      - obs-net
    restart: unless-stopped

  # ─── Seq (structured log aggregation) ──────────────────────────────────
  seq:
    image: datalust/seq:latest
    container_name: seq
    ports:
      - "8888:80"    # UI
      - "5341:5341"  # Ingestion
    environment:
      - ACCEPT_EULA=Y
      - SEQ_FIRSTRUN_NOAUTHENTICATION=true
    volumes:
      - seq-data:/data
    networks:
      - obs-net
    restart: unless-stopped

volumes:
  seq-data:

networks:
  obs-net:
    driver: bridge
